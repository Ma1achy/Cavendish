[package]
name = "compute"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
gravity = { path = "../gravity" }
source = { path = "../source" }
instrument = { path = "../instrument" }
math = { path = "../math" }
