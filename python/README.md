# python — the Cavendish SDK package

The Python surface over the Rust engine (PyO3 + torch hand-off). Built with maturin
against `crates/sdk`. Empty until **M7** (`milestones/M7-sdk-python-torch.md`); the
CI `python` job is skipped until `crates/sdk/pyproject.toml` exists.
