# Historical notes — superseded

Everything in this folder is **historical** and is *not* part of the design contract.
The scratchpads were thinking-in-progress that has since been folded into the spec
(`cavendish-spec/cavendish.tex`) and the subsystem drill-downs (`design/*.md`).
`SPEC.md` and `ARCHITECTURE.md` are retired tombstones. `demo.py` / `forward.py` are
early Python prototypes.

Kept only to trace *why* decisions were made. If it conflicts with the spec, the spec wins.
